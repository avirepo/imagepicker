<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:padding="6dp">

    <TextView
        android:id="@+id/txv_alert_dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:singleLine="true"
        android:textColor="@color/app_blue"
        android:textSize="@dimen/text_size_large"
        tools:text="Title"/>

    <TextView
        android:id="@+id/txv_alert_dialog_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:gravity="left"
        android:layout_marginTop="@dimen/space_smallest"
        android:textColor="@color/app_blue"
        android:textSize="@dimen/text_size_small"
        tools:ignore="RtlHardcoded"
        tools:text="You have enter wrong value"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_alert_dialog_cancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:padding="@dimen/space_default"
            android:text="@string/cancel"/>

        <Button
            android:id="@+id/btn_alert_dialog_done"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_weight="1"
            android:padding="@dimen/space_default"
            android:text="@string/done"
            tools:ignore="RtlHardcoded"/>
    </LinearLayout>

</LinearLayout>